services:
  postgres:
    image: postgres:16
    container_name: chat_postgres
    environment:
      POSTGRES_DB: chatdb
      POSTGRES_USER: chat
      POSTGRES_PASSWORD: chatpass
    ports:
      - "5432:5432"
    volumes:
      - chat_pgdata:/var/lib/postgresql/data

volumes:
  chat_pgdata: